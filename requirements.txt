# ── Core LLM / RAG stack ──────────────────────────────────────────────────────
langchain>=0.2.0
langchain-groq>=0.1.6
langchain-huggingface>=0.0.3
langchain-chroma>=0.1.2
langchain-text-splitters>=0.2.0
langchain-core>=0.2.0

# ── Vector store ───────────────────────────────────────────────────────────────
chromadb>=0.5.0
pysqlite3-binary>=0.5.0; sys_platform == "linux"   # auto-skipped on Windows/macOS

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=2.6.0
huggingface-hub>=0.22.0

# ── Scraping – Strategy 1: plain requests + HTML parsing ──────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0                      # faster BS4 parser
fake-useragent>=1.4.0

# ── Scraping – Strategy 2: Cloudflare / JS-challenge bypass ───────────────────
cloudscraper>=1.2.71

# ── Scraping – Strategy 3: intelligent content extractor ──────────────────────
trafilatura>=1.9.0

# ── Scraping – Strategy 4: article / blog / review parser ────────────────────
newspaper3k>=0.2.8
nltk>=3.8.1                      # required by newspaper3k

# ── Scraping – Strategy 5: headless browser (optional, for JS-heavy sites) ────
# Selenium requires ChromeDriver installed separately (not via pip).
# Uncomment if you want headless-browser fallback:
# selenium>=4.20.0
# webdriver-manager>=4.0.1        # auto-downloads matching ChromeDriver

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
json-repair>=0.25.0          # auto-fixes malformed LLM JSON output
urllib3>=2.2.0
